# Tester Handoff: Hero Model Carousel Review

Review scope:

- Confirm the hero visual in `index.html` uses model images from `assets/models/`.
- Confirm the hero carousel has an automatic 3000ms interval.
- Confirm previous and next controls exist and do not trigger gallery navigation.
- Confirm clicking the hero visual links to `gallery.html#...` for the currently displayed model.
- Confirm all referenced model images exist and paths are project-local relative paths.

Tester pass is evidence only and is not release permission.
