# Tester Handoff: Clickable Model Gallery Review

Review scope:

- Confirm the 8 model cards in `index.html` link to `gallery.html` with model-specific hashes.
- Confirm `gallery.html` includes all local images from the 8 `assets/models/` subfolders.
- Confirm every gallery image receives a lower-right `价格` button.
- Confirm all image references are project-local relative paths and files exist.
- Confirm unknown price/inventory/commercial facts remain `to-be-confirmed`.

Tester pass is evidence only and is not release permission.
